# websocket-ping

## Setup

```sh
$ python3 -m venv .venv
$ . .venv/bin/activate
$ pip install -r requirements.txt
$ python server.py
```

Open `'localhost:8000'` in a browser and stare at your terminal.
